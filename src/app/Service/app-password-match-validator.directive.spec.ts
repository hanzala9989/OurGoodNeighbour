import { AppPasswordMatchValidatorDirective } from './app-password-match-validator.directive';

describe('AppPasswordMatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPasswordMatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
